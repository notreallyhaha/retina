[phases.setup]
nixPkgs = ["python311"]
aptPkgs = ["libgl1", "libglib2.0-0", "libsm6", "libxrender1", "libxext6", "libgomp1", "libxcb1"]

[start]
cmd = "cd server && uvicorn main:app --host 0.0.0.0 --port $PORT"
